# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/book-manager?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/book-manager

# Generate a strong random string for production (e.g. openssl rand -base64 32)
=your-super-secret-jwt-key-change-in-production
JWT_SECRET